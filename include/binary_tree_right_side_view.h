//
// Created by 盛枫 on 2024/10/11.
//

std::vector<int> rightSideView(TreeNode* root) {
    if(root == nullptr) {
        return vector<int>();
    }

    queue<TreeNode*> que;
    que.push(root);
    std::vector<int> ans;

    while(!que.empty()) {

    }
}